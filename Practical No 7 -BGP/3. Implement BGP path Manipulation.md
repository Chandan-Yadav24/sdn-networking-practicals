**3. Implement BGP path Manipulation** 



**Part 1: Build the Network and Configure Basic Device Settings and Interface** 

**Addressing**  

**Step 1: Topology** 

**Step 2: Configure basic settings for each router.** 

**• Router R1** 

enable 

configure terminal 

line con 0 

exec-timeout 0 0 

logging synchronous 

exit 

banner motd # This is R1, BGP Path Manipulation Lab # 

ipv6 unicast-routing 

interface FastEthernet0/0 

ip address 10.1.2.1 255.255.255.0 

ipv6 address FE80::1:1 link-local 

ipv6 address 2001:DB8:ACAD:1012::1/64 

no shutdown 

exit 

interface Serial0/0 

ip address 10.1.3.1 255.255.255.128 

ipv6 address FE80::1:2 link-local 

ipv6 address 2001:DB8:ACAD:1013::1/64 

clock rate 64000 

no shutdown 

exit 

interface Serial0/1 

ip address 10.1.3.129 255.255.255.128 

ipv6 address FE80::1:3 link-local 

ipv6 address 2001:DB8:ACAD:1014::1/64 

clock rate 64000 

no shutdown 

exit 

interface Loopback0 

ip address 192.168.1.1 255.255.255.224 

ipv6 address FE80::1:4 link-local 

ipv6 address 2001:DB8:ACAD:1000::1/64 

no shutdown 

exit 

interface Loopback1 

ip address 192.168.1.65 255.255.255.192 

ipv6 address FE80::1:5 link-local 

ipv6 address 2001:DB8:ACAD:1001::1/64 

no shutdown 

exit 

end 

write memory 

&nbsp;

&nbsp;

&nbsp;

**R2** 

enable 

configure terminal 

&nbsp;

no ip domain lookup 

&nbsp;

line con 0 

exec-timeout 0 0 

logging synchronous 

exit 

banner motd # This is R2, BGP Path Manipulation Lab # 

ipv6 unicast-routing 

interface FastEthernet0/0 

ip address 10.1.2.2 255.255.255.0 

ipv6 address FE80::2:1 link-local 

ipv6 address 2001:DB8:ACAD:1012::2/64 

no shutdown 

exit 

interface FastEthernet0/1 

ip address 10.2.3.2 255.255.255.0 

ipv6 address FE80::2:2 link-local 

ipv6 address 2001:DB8:ACAD:1023::2/64 

no shutdown 

exit 

interface Loopback0 

ip address 192.168.2.1 255.255.255.224 

ipv6 address FE80::2:3 link-local 

ipv6 address 2001:DB8:ACAD:2000::2/64 

no shutdown 

exit 

interface Loopback1 

ip address 192.168.2.65 255.255.255.192 

ipv6 address FE80::2:4 link-local 

ipv6 address 2001:DB8:ACAD:2001::2/64 

no shutdown 

exit 

end 

write memory 



**Router R3** 

enable 

configure terminal 

no ip domain lookup 

line con 0 

exec-timeout 0 0 

logging synchronous 

exit 

banner motd # This is R3, BGP Path Manipulation Lab # 

ipv6 unicast-routing 

interface FastEthernet0/0 

ip address 10.2.3.3 255.255.255.0 

ipv6 address FE80::3:1 link-local 

ipv6 address 2001:DB8:ACAD:1023::3/64 

no shutdown 

exit 

interface Serial0/0 

ip address 10.1.3.3 255.255.255.128 

ipv6 address FE80::3:2 link-local 

ipv6 address 2001:DB8:ACAD:1013::3/64 

clock rate 64000 

no shutdown 

exit 

interface Serial0/1 

ip address 10.1.3.130 255.255.255.128 

ipv6 address FE80::3:3 link-local 

ipv6 address 2001:DB8:ACAD:1014::3/64 

clock rate 64000 

no shutdown 

exit 

interface Loopback0 

ip address 192.168.3.1 255.255.255.224 

ipv6 address FE80::3:4 link-local 

ipv6 address 2001:DB8:ACAD:3000::1/64 

no shutdown 

exit 

interface Loopback1 

ip address 192.168.3.65 255.255.255.192 

ipv6 address FE80::3:5 link-local 

ipv6 address 2001:DB8:ACAD:3001::1/64 

no shutdown 

exit 

end 

write memory 



**Part 2: Configure and Verify Multi-Protocol BGP on all Routers** 

**Step 1: On R1, create the core BGP configuration.** 

R1#configure terminal 

router bgp 6500 

bgp router-id 1.1.1.1 

no bgp default ipv4-unicast 

neighbor 10.1.2.2 remote-as 500 

neighbor 10.1.3.3 remote-as 300 

neighbor 10.1.3.130 remote-as 300 

neighbor 2001:DB8:ACAD:1012::2 remote-as 500 

neighbor 2001:DB8:ACAD:1013::3 remote-as 300 

neighbor 2001:DB8:ACAD:1014::3 remote-as 300 

end 

write memory 



**Step 2: On R1, configure the IPv4 unicast address family.** 

R1#configure terminal 

router bgp 6500 

address-family ipv4 unicast 

network 192.168.1.0 mask 255.255.255.224 

network 192.168.1.64 mask 255.255.255.192 

no neighbor 2001:DB8:ACAD:1012::2 activate 

no neighbor 2001:DB8:ACAD:1013::3 activate 

no neighbor 2001:DB8:ACAD:1014::3 activate 

neighbor 10.1.2.2 activate 

neighbor 10.1.3.3 activate 

neighbor 10.1.3.130 activate 

exit-address-family 

end 

write memory 



**Step 3: On R1, configure the IPv6 unicast address family.**

 

R1#configure terminal 

router bgp 6500 

address-family ipv6 unicast 

network 2001:DB8:ACAD:1000::/64 

network 2001:DB8:ACAD:1001::/64 

neighbor 2001:DB8:ACAD:1012::2 activate 

neighbor 2001:DB8:ACAD:1013::3 activate 

neighbor 2001:DB8:ACAD:1014::3 activate 

exit-address-family 

end 

write memory 



**Step 4: Configure MP-BGP on R2 and R3 as you did in the previous step**. 



conf t 

router bgp 500 

bgp router-id 2.2.2.2 

no bgp default ipv4-unicast 

neighbor 10.1.2.1 remote-as 6500 

neighbor 10.2.3.3 remote-as 300 

neighbor 2001:db8:acad:1012::1 remote-as 6500 

neighbor 2001:db8:acad:1023::3 remote-as 300 

address-family ipv4 

network 192.168.2.0 mask 255.255.255.224 

network 192.168.2.64 mask 255.255.255.192 

neighbor 10.1.2.1 activate 

neighbor 10.2.3.3 activate 

exit-address-family 

address-family ipv6 

network 2001:db8:acad:2000::/64 

network 2001:db8:acad:2001::/64 

neighbor 2001:db8:acad:1012::1 activate 

neighbor 2001:db8:acad:1023::3 activate 

exit-address-family 

end 

Wr 



**Router R3:** 

conf t 

router bgp 300 

bgp router-id 3.3.3.3 

no bgp default ipv4-unicast 

neighbor 10.1.3.1 remote-as 6500 

neighbor 10.1.3.129 remote-as 6500 

neighbor 10.2.3.2 remote-as 500 

neighbor 2001:db8:acad:1013::1 remote-as 6500 

neighbor 2001:db8:acad:1014::1 remote-as 6500 

neighbor 2001:db8:acad:1023::2 remote-as 500 

address-family ipv4 

network 192.168.3.0 mask 255.255.255.224 

network 192.168.3.64 mask 255.255.255.192 

neighbor 10.1.3.1 activate 

neighbor 10.1.3.129 activate 

neighbor 10.2.3.2 activate 

exit-address-family 

address-family ipv6 

network 2001:db8:acad:3000::/64 

network 2001:db8:acad:3001::/64 

neighbor 2001:db8:acad:1013::1 activate 

neighbor 2001:db8:acad:1014::1 activate 

neighbor 2001:db8:acad:1023::2 activate 

exit-address-family 

end 

Wr

&nbsp;

**Step 5: Verify that MP-BGP is operational.** 



R1#show bgp ipv4 unicast summary 

R1#show bgp ipv6 unicast summary 

R1#show bgp ipv4 unicast | begin Network 

R1#show bgp ipv6 unicast | begin Network 



**• View Routing Tables:** 

**R1#show ipv6 route bgp** 



**Part 3: Configure and Verify BGP Path Manipulation Settings on all Routers** 

**Step 1: Configure ACL-based route filtering.** 

**On R1, issue the command show bgp ipv4 unicast i 300 to see what prefixes ASN300** 

**sharing via BGP. Take note of those prefixes that do not originate in ASN300.** 



R1#show bgp ipv4 unicast | i 300 

R1#show bgp ipv4 unicast begin 192.168.3 



**• On R3, configure an access list designed to match the source address and mask of the** 

**networks belonging to ASN300:** 



conf t 

ip access-list extended ALLOWED\_TO\_R! 

permit ip 192.168.3.0 0.0.0.0 255.255.255.224 0.0.0.0 

permit ip 192.168.3.64 0.0.0.0 255.255.255.192 0.0.0.0 

exit 

end 

Wr 



**• On R3, apply the ALLOWED\_TO\_R1 ACL as a distribute list to the IPv4 neighbor** 

**adjacencies with R1.**

&nbsp;

enable 

configure terminal 

router bgp 300 

address-family ipv4 unicast 

neighbor 10.1.3.1 distribute-list ALLOWED\_TO\_R1 out 

neighbor 10.1.3.129 distribute-list ALLOWED\_TO\_R1 out 

exit-address-family 

end 

write memory 



**Perform a reset of the IPv4 adjacency with R1 for the outbound traffic without tearing** 

**down** 

**the session.** 



R3#clear bgp ipv4 unicast 6500 out 



**On R1, issue the command show bgp ipv4 unicast i 300 to see what prefixes routes** 

**ASN300** 

**is now sharing via BGP. All of the prefixes should now originate in ASN300:** 



R1#show bgp ipv4 unicast | i 300 



**Step 2: Configure prefix-list-based route filtering.** 

**On R1, issue the command show bgp ipv4 unicast begin 192.168.3 to see what prefixes** 

**ASN500 is sharing via BGP. Take note of those prefixes that do not originate in** 

**ASN500.** 



R1#show bgp ipv4 unicast | begin 192.168.3 



On R1, configure AS-PATH ACL to match the routes from the local ASN. 

ip prefix-list ALLOWED\_FROM\_R2 seq 5 permit 192.168.2.0/24 le 27 

On R1, apply the AS-PATH ACL as a filter-list on the adjacency configured with R2.

**r1** 

conf t 

router bgp 6500 

address-family ipv4 unicast 

neighbor 10.1.2.2 prefix-list ALLOWED\_FROM\_R2 in 

exit-address-family 

end 

wr 



**On R1, perform a reset of the IPv4 adjacency with R2 for the outbound traffic without** 

**tearing down the session** 



R1#clear bgp ipv4 unicast 500 in 

• On R1, issue the command show bgp ipv4 unicast i 500 to see what prefixes routes 

ASN500 

is now sharing via BGP. All of the prefixes should now originate in ASN500. 

R1#show bgp ipv4 unicast | i 500 

• On R2, issue the command show bgp ipv4 unicast i 6500 to see what prefixes routes 

ASN6500 is now sharing via BGP. All of the prefixes should now originate in ASN6500. 

Step 3: Configure an AS-PATH ACL to filter routes being advertised. 

• On R2, issue the command show bgp ipv4 unicast begin Network to see what prefixes 

ASN6500 is sharing via BGP. 

R2#show bgp ipv4 unicast | begin Network 

• On R1, configure AS-PATH ACL to match the routes from the local ASN. 

conf t 

ip as-path access-list 1 permit ^$ 

end 

• On R1, apply the AS-PATH ACL as a filter-list on the adjacency configured with R2. 

conf t 

router bgp 6500 

address-family ipv4 unicast 

neighbor 10.1.2.2 filter-list 1 out 

end 

• On R1, perform a reset of the IPv4 adjacency with R2 for the outbound traffic without 

tearing down the session 

R1#clear bgp ipv4 unicast 500 out 

• On R2, issue the command show bgp ipv4 unicast i 6500 to see what prefixes routes 

ASN6500 is now sharing via BGP. All of the prefixes should now originate in ASN6500. 

R2#show bgp ipv4 unicast i 6500 

Step 4: Configure IPv6 prefix-list-based route filtering. 

• On R1, issue the command show bgp ipv6 unicast neighbors 2

001:db8:acad: 1012::2 

routes to see what IPv6 prefixes ASN500 is sharing via BGP. 

show bgp ipv6 unicast neighbors 2001:db8:acad:1012::2 routes

&nbsp;

**• On R1, configure an IPv6 prefix list designed to match the source address and mask of** 

**networks belonging to ASN500.**

&nbsp;

conf t 

ipv6 prefix-list IPV6\_ALLOWED\_FROM\_R2 seq 5 permit 2001:db8:acad:2000::/64 

ipv6 prefix-list IPV6\_ALLOWED\_FROM\_R2 seq 10 permit 2001:db8:acad:2001::/64 

end 

wr 



**Apply the IPV6\_ALLOWED\_FROM\_R2 prefix list to the IPv6 neighbor adjacencies** 

**for R2** 



conf t 

router bgp 6500 

address-family ipv6 unicast 

neighbor 2001:db8:acad:1012::2 prefix-list IPV6\_ALLOWED\_FROM\_R2 in 

exit-address-family 

end 

wr 



**• Perform a reset of the IPv6 adjacency with R2 for the inbound traffic without tearing** 

**down the session.** 



R1#clear bgp ipv6 unicast 500 in 



**• On R1, issue the command show bgp ipv6 unicast neighbors 2001:db8:acad:1012::2** 

**routes to see what IPv6 prefixes routes ASN500 is now sharing via BGP. All of the IPv6** 

**prefixes should now originate in ASN500.** 



R1#show bgp ipv6 unicast neighbors 2001: db8: acad:1012::2 routes

&nbsp;

**Step 5: Configure BGP path attribute manipulation to effect routing** 



R1#show bgp ipv4 unicast 192.168.3.0 

**• On R1, configure a prefix list designed to match the source address and mask of** 

**networks** 

**belonging to ASN300.** 



conf t 

ip prefix-list PREFERRED\_IPV4\_PATH seq 5 permit 192.168.3.0/24 le 27 

end 

wr 



**• Create a route-map named USE\_THIS\_PATH\_FOR\_IPV4 that matches on the prefix** 

**list you** 

**just created and sets the local preference to 250.** 



conf t 

route-map USE\_THIS\_PATH\_FOR\_IPV4 permit 10 

match ip address prefix-list PREFERRED\_IPV4\_PATH 

set local-preference 250 

exit 

end 

wr 

**• Next, apply this route map to the BGP neighbor 10.1.3.130. LITLET Comiguration** 

**Commands, Une per e** 



conf t 

router bgp 6500 

address-family ipv4 unicast 

neighbor 10.1.3.130 route-map USE\_THIS\_PATH\_FOR\_IPV4 in 

exit-address-family 

end 

wr 



**• Perform a reset of the IPv4 adjacency with R3 for the inbound traffic without tearing** 

**down** 

**the session.** 



R1#clear bgp ipv4 unicast 300 in 



**• On R1, issue the command show ip route bgp and take note of the next hop addresses** 

**for the** 

**192.168.3.0/27 and 192.168.3.64/26 networks; it should be 10.1.3.130 for both. Issue the** 

**command show bgp ipv4 unicast and you should see the local preference value in the** 

**appropriate column.** 



R1#show ip route bgp 

R1#show bgp ipv4 unicast begin Network 

**• Final Config R1:** 

R1#show run 

Final Config R2: 

Building configuration... 

R2#show run 

**• Final Config R3:** 

R3#show run 

&nbsp;

&nbsp;

&nbsp;



